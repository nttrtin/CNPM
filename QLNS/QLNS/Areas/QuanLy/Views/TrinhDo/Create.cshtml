@model QLNS.Models.TrinhDo


@{
    ViewData["Title"] = "Thêm Trình Độ";
}
<div class="card">
    <h5 class="card-header text-center">@ViewData["Title"] </h5>
    <div class="card-body">
        <form asp-action="Create">
            <div class="mb-3">
                <label asp-for="MaTrinhDo" class="control-label"></label>
                <input asp-for="MaTrinhDo" class="form-control" />
                <span asp-validation-for="MaTrinhDo" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <label asp-for="TenTrinhDo" class="control-label"></label>
                <input asp-for="TenTrinhDo" class="form-control" />
                <span asp-validation-for="TenTrinhDo" class="text-danger"></span>
            </div>
            <div class="mb-0">
                <button type="submit" class="btn btn-primary"><i class="fa-light fa-floppy-disk"></i> Thêm mới</button> hoặc <a asp-action="Index">Quay về danh sách</a>
            </div>
        </form>
    </div>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}