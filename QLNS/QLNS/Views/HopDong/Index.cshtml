@model IEnumerable<QLNS.Models.HopDong>

@{
    ViewData["Title"] = "Index";
}

<div class="card">
    <h2 class="card-header text-center text-light bg-dark" style="font-weight: 500;font-family:none;">@ViewData["Title"]</h2>
    <div class="card-body">
        <div class="row mb-3">
            <div class="col-md-6">
                <p><a asp-action="Create" class="btn btn-success"><i class="fa-soild fa-plus"></i> Thêm mới</a></p>
            </div>
            <div class="col-md-6 text-end">
                <div id="export-buttons"></div>
            </div>
        </div>
        <table id="datatable" class="table table-sm table-hover table-striped table-bordered mb-0">
            <thead>
                <tr>
                  
                    <th class="align-middle text-center">  @Html.DisplayNameFor(model => model.MaHopDong)</th>
                    <th class="align-middle text-center">   @Html.DisplayNameFor(model => model.TenHopDong)</th>
                    <th class="align-middle text-center">  @Html.DisplayNameFor(model => model.NgayBatDau)</th>
                    <th class="align-middle text-center">    @Html.DisplayNameFor(model => model.NgayKetThuc)</th>
                    <th class="align-middle text-center">   @Html.DisplayNameFor(model => model.NoiDung)</th>
                    <th class="align-middle text-center">  @Html.DisplayNameFor(model => model.NgayKy)</th>
                    <th class="align-middle text-center">  @Html.DisplayNameFor(model => model.NhanVien.TenNhanVien)</th>
                 
                    <th width="5%" class="align-middle text-center">Sửa</th>
                    <th width="5%" class="align-middle text-center">Xóa</th>
                </tr>
            </thead>
            <tbody>
                @{
                    var stt = 1;
                }
                @foreach (var item in Model)
                {
                    <tr>
                   
                        <td class="align-middle text-center"> @Html.DisplayFor(modelItem => item.MaHopDong)</td>
                        <td class="align-middle text-center"> @Html.DisplayFor(modelItem => item.TenHopDong)</td>
                        <td class="align-middle text-center">  @Html.DisplayFor(modelItem => item.NgayBatDau)</td>
                        <td class="align-middle text-center"> @Html.DisplayFor(modelItem => item.NgayKetThuc)</td>
                        <td class="align-middle text-center">@Html.DisplayFor(modelItem => item.NoiDung)</td>
                        <td class="align-middle text-center"> @Html.DisplayFor(modelItem => item.NgayKy)</td>
                        <td class="align-middle text-center"> @Html.DisplayFor(modelItem => item.NhanVien.TenNhanVien)</td>
               
                        <td class="align-middle text-center"><a asp-action="Edit" asp-route-id="@item.ID"><i class="fa-light fa-pen-to-square"></i></a></td>
                        <td class="align-middle text-center"><a asp-action="Delete" asp-route-id="@item.ID"><i class="fa-light fa-trash-can text-danger"></i></a></td>
                    </tr>
                    stt++;
                }
            </tbody>
        </table>
    </div>
</div>
